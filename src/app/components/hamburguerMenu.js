"use client";
import React from "react";

export default function HamburguerMenu({ onClick }) {


    return (
        <span onClick={onClick} id="hamburguer-menu" className="material-symbols-outlined">
            dehaze
        </span>
    )
}